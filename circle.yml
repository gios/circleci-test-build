machine:
  node:
    version: 8.1.2

dependencies:
  pre:
    - cd ui && npm i
    - cd ui && ./node_modules/.bin/ng build
  post:
    - mkdir static
    - cp -v ui/dist/* static
    - ls -a
    - cd static && ls -a

deployment:
  staging:
    branch: master
    heroku:
      appname: mom-and-i