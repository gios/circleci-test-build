dependencies:
  pre:
    - ls -a
    - cd models
    - ls -a
    - npm i
    - ./node-modules/ng build
    - mv dist ../public

deployment:
  staging:
    branch: master
    heroku:
      appname: mom-and-i