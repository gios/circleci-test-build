machine:
  node:
    version: 8.1.2

dependencies:
  pre:
    - cd ui && npm i
    - cd ui && ./node_modules/.bin/ng build
    - mkdir public
    - mv -v ui/dist/* public
    - ls -a
    - cd public && ls -a

deployment:
  staging:
    branch: master
    heroku:
      appname: mom-and-i