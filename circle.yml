machine:
  node:
    version: 8.1.2

dependencies:
  pre:
    - cd ui && npm i
    - cd ui && ./node_modules/.bin/ng build
    - mv dist ../public

deployment:
  staging:
    branch: master
    heroku:
      appname: mom-and-i